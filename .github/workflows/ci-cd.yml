name: Web Calculator CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  DOTNET_VERSION: '8.0.x'
  PROJECT_PATH: 'src/WebCalculator'  # ИСПРАВЛЕНО!
  TEST_PATH: 'tests/WebCalculator.Tests'  # ИСПРАВЛЕНО!

jobs:
  setup:
    name: 💻 Подготовка окружения
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Загружаем код из репозитория
      uses: actions/checkout@v4

    - name: 📁 Проверяем структуру проекта
      run: |
        echo "Проверяем наличие всех файлов..."
        find . -type f -name "*.csproj" | head -20
        echo "---"
        ls -la src/ || echo "Папки src не существует"
        echo "---"
        ls -la tests/ || echo "Папки tests не существует"

    - name: ⚙️ Устанавливаем .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 📦 Восстанавливаем зависимости
      run: |
        # Сначала находим все .csproj файлы
        echo "Найденные .csproj файлы:"
        find . -name "*.csproj" -type f
        
        echo "---"
        echo "Восстанавливаем зависимости..."
        # Восстанавливаем все проекты
        dotnet restore || echo "Ошибка при restore"
        
        echo "---"
        echo "Проверяем восстановление..."
        find . -name "project.assets.json" -type f | head -10

  build:
    name: 🔨 Сборка проекта
    needs: setup
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Загружаем код
      uses: actions/checkout@v4

    - name: ⚙️ Устанавливаем .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 📦 Восстанавливаем зависимости
      run: dotnet restore

    - name: 🔨 Собираем проект
      run: |
        # Проверяем существование проекта
        if [ ! -f "src/WebCalculator/WebCalculator.csproj" ]; then
          echo "Файл проекта не найден! Ищем..."
          find . -name "*.csproj" -type f
          exit 1
        fi
        
        echo "Собираем проект..."
        dotnet build src/WebCalculator --configuration Release

    - name: 🔍 Проверяем результат сборки
      run: |
        echo "Проверяем собранные файлы..."
        if [ -d "src/WebCalculator/bin/Release/net8.0/" ]; then
          ls -la src/WebCalculator/bin/Release/net8.0/
        else
          echo "Папка сборки не найдена, ищем..."
          find . -name "*.dll" -type f | head -10
        fi

    - name: 💾 Сохраняем собранное приложение
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: web-calculator-app
        path: src/WebCalculator/bin/Release/net8.0/
        retention-days: 7

  test:
    name: ✓ Запуск тестов
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Загружаем код
      uses: actions/checkout@v4

    - name: ⚙️ Устанавливаем .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 📦 Восстанавливаем зависимости
      run: dotnet restore

    - name: ✓ Запускаем unit-тесты
      run: |
        # Сначала проверяем наличие тестового проекта
        if [ ! -f "tests/WebCalculator.Tests/WebCalculator.Tests.csproj" ]; then
          echo "Тестовый проект не найден! Ищем..."
          find . -name "*.Tests.csproj" -type f
          exit 1
        fi
        
        echo "Запускаем тесты..."
        dotnet test tests/WebCalculator.Tests --verbosity normal --logger "trx"

    - name: 📊 Сохраняем результаты тестов
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: tests/WebCalculator.Tests/TestResults/
        retention-days: 30

  package:
    name: 📦 Создание пакета
    needs: test
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Загружаем код
      uses: actions/checkout@v4

    - name: ⚙️ Устанавливаем .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 📦 Восстанавливаем зависимости
      run: dotnet restore

    - name: 📦 Публикуем приложение
      run: |
        dotnet publish src/WebCalculator -c Release -o ./publish --self-contained false

    - name: 📁 Архивируем приложение
      run: |
        mkdir -p artifacts
        zip -r artifacts/web-calculator-v1.0.0.zip ./publish/*
        echo "Размер архива:"
        ls -lh artifacts/web-calculator-v1.0.0.zip

    - name: 💾 Сохраняем релизный пакет
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: release-package
        path: artifacts/web-calculator-v1.0.0.zip
        retention-days: 30